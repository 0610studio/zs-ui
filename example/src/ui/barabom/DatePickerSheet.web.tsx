import type { DateString } from '@0610studio/zs-ui';

type Props = {
  initialDate: DateString;
  onConfirm: (date: DateString) => void;
};

export default function DatePickerSheet(_props: Props) {
  return null;
}
